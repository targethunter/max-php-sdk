# v2.0.0 Strict Docs Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the `v2.0.0` breaking release that keeps only documented MAX API methods, adds missing documented SDK surface, and updates production guidance.

**Architecture:** Keep the existing module layout and DTO style. Remove unsupported public methods directly from existing modules, add small focused classes for upload type constants, clipboard buttons, and webhook secret verification, then document the migration path in README.

**Tech Stack:** PHP 7.4+/8.x, Guzzle 7, PHPUnit 9/10/11, Composer PSR-4 autoloading.

---

## File Structure

- Modify `src/Client/Modules/Chats/Chats.php`: remove unsupported documented-breaking methods `getAll()` and `delete()`.
- Modify `src/Client/Modules/Bots/Bots.php`: remove unsupported `update()` method.
- Create `src/Config/UploadTypes.php`: constants for documented upload types only.
- Create `src/Client/DTO/Messages/Attachments/Buttons/ClipboardButton.php`: DTO for documented `clipboard` inline keyboard button.
- Create `src/Webhook/WebhookSecretVerifier.php`: timing-safe validation helper for `X-Max-Bot-Api-Secret`.
- Create `tests/StrictDocsSyncTest.php`: method removal and new surface tests.
- Create `tests/WebhookSecretVerifierTest.php`: webhook helper tests.
- Modify `README.md`: migration notes and MAX production guidance.

No transport rewrite, rate limiter, or broad DTO refactor is part of this plan.

---

### Task 1: Remove Unsupported Public Methods

**Files:**
- Modify: `src/Client/Modules/Chats/Chats.php`
- Modify: `src/Client/Modules/Bots/Bots.php`
- Test: `tests/StrictDocsSyncTest.php`

- [ ] **Step 1: Write failing tests for removed methods**

Create `tests/StrictDocsSyncTest.php`:

```php
<?php

declare(strict_types=1);

namespace TH\MAX\Tests;

use PHPUnit\Framework\TestCase;
use TH\MAX\Client\DTO\Messages\Attachments\Buttons\ClipboardButton;
use TH\MAX\Client\Modules\Bots\Bots;
use TH\MAX\Client\Modules\Chats\Chats;
use TH\MAX\Config\UploadTypes;

class StrictDocsSyncTest extends TestCase
{
    public function testUnsupportedMethodsAreRemovedFromPublicModules(): void
    {
        $this->assertFalse(method_exists(Chats::class, 'getAll'));
        $this->assertFalse(method_exists(Chats::class, 'delete'));
        $this->assertFalse(method_exists(Bots::class, 'update'));
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
vendor/bin/phpunit tests/StrictDocsSyncTest.php --filter testUnsupportedMethodsAreRemovedFromPublicModules
```

Expected: FAIL because `Chats::getAll()`, `Chats::delete()`, and `Bots::update()` still exist.

- [ ] **Step 3: Remove unsupported methods**

In `src/Client/Modules/Chats/Chats.php`, delete the full `getAll()` method:

```php
public function getAll(int $count = 50, ?int $marker = null): ChatListResponse
{
    $response = $this->getRequest('/chats', [
        'count' => $count,
        'marker' => $marker
    ]);

    return new ChatListResponse($response);
}
```

Also delete the full `delete()` method:

```php
public function delete(int $chat_id): ResultResponse
{
    return new ResultResponse(
        $this->deleteRequest('/chats/' . $chat_id)
    );
}
```

Remove the unused import after deleting `getAll()`:

```php
use TH\MAX\Client\DTO\Chats\Response\ChatListResponse;
```

In `src/Client/Modules/Bots/Bots.php`, delete the full `update()` method:

```php
public function update(
    ?string $first_name = null,
    ?string $last_name = null,
    ?string $name = null,
    ?string $description = null,
    ?array $commands = null,
    ?string $photo = null
): Bot {
    return new Bot($this->patchRequest('/me', [
        'first_name' => $first_name,
        'last_name' => $last_name,
        'name' => $name,
        'description' => $description,
        'commands' => $commands,
        'photo' => $photo
    ])
    );
}
```

- [ ] **Step 4: Run focused test**

Run:

```bash
vendor/bin/phpunit tests/StrictDocsSyncTest.php --filter testUnsupportedMethodsAreRemovedFromPublicModules
```

Expected: PASS.

- [ ] **Step 5: Commit**

Run:

```bash
git add src/Client/Modules/Chats/Chats.php src/Client/Modules/Bots/Bots.php tests/StrictDocsSyncTest.php
git commit -m "Remove unsupported MAX API methods for v2"
```

---

### Task 2: Add Upload Type Constants

**Files:**
- Create: `src/Config/UploadTypes.php`
- Modify: `tests/StrictDocsSyncTest.php`

- [ ] **Step 1: Add failing upload constants test**

Append this method to `StrictDocsSyncTest`:

```php
public function testUploadTypesExposeOnlyDocumentedTypes(): void
{
    $reflection = new \ReflectionClass(UploadTypes::class);

    $this->assertSame([
        'IMAGE' => 'image',
        'VIDEO' => 'video',
        'AUDIO' => 'audio',
        'FILE' => 'file',
    ], $reflection->getConstants());

    $this->assertFalse($reflection->hasConstant('PHOTO'));
}
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
vendor/bin/phpunit tests/StrictDocsSyncTest.php --filter testUploadTypesExposeOnlyDocumentedTypes
```

Expected: FAIL because `TH\MAX\Config\UploadTypes` does not exist.

- [ ] **Step 3: Create UploadTypes class**

Create `src/Config/UploadTypes.php`:

```php
<?php

namespace TH\MAX\Config;

class UploadTypes
{
    public const IMAGE = 'image';
    public const VIDEO = 'video';
    public const AUDIO = 'audio';
    public const FILE = 'file';
}
```

- [ ] **Step 4: Run focused test**

Run:

```bash
vendor/bin/phpunit tests/StrictDocsSyncTest.php --filter testUploadTypesExposeOnlyDocumentedTypes
```

Expected: PASS.

- [ ] **Step 5: Commit**

Run:

```bash
git add src/Config/UploadTypes.php tests/StrictDocsSyncTest.php
git commit -m "Add documented upload type constants"
```

---

### Task 3: Add Clipboard Button DTO

**Files:**
- Create: `src/Client/DTO/Messages/Attachments/Buttons/ClipboardButton.php`
- Modify: `tests/StrictDocsSyncTest.php`

- [ ] **Step 1: Add failing clipboard button test**

Append this method to `StrictDocsSyncTest`:

```php
public function testClipboardButtonSerializesToDocumentedShape(): void
{
    $button = new ClipboardButton([
        'text' => 'Copy',
        'payload' => 'PROMO-123',
    ]);

    $this->assertSame([
        'type' => 'clipboard',
        'text' => 'Copy',
        'payload' => 'PROMO-123',
    ], $button->toArray());
}
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
vendor/bin/phpunit tests/StrictDocsSyncTest.php --filter testClipboardButtonSerializesToDocumentedShape
```

Expected: FAIL because `ClipboardButton` does not exist.

- [ ] **Step 3: Create ClipboardButton DTO**

Create `src/Client/DTO/Messages/Attachments/Buttons/ClipboardButton.php`:

```php
<?php

namespace TH\MAX\Client\DTO\Messages\Attachments\Buttons;

use TH\MAX\DTO\BaseDTO;

class ClipboardButton extends BaseDTO
{
    public string $type = 'clipboard';

    /**
     * От 1 до 128 символов.
     *
     * Видимый текст кнопки.
     */
    public string $text;

    /**
     * Текст, который будет скопирован в буфер обмена.
     */
    public string $payload;
}
```

- [ ] **Step 4: Run focused test**

Run:

```bash
vendor/bin/phpunit tests/StrictDocsSyncTest.php --filter testClipboardButtonSerializesToDocumentedShape
```

Expected: PASS.

- [ ] **Step 5: Commit**

Run:

```bash
git add src/Client/DTO/Messages/Attachments/Buttons/ClipboardButton.php tests/StrictDocsSyncTest.php
git commit -m "Add clipboard inline keyboard button"
```

---

### Task 4: Add Webhook Secret Verification Helper

**Files:**
- Create: `src/Webhook/WebhookSecretVerifier.php`
- Create: `tests/WebhookSecretVerifierTest.php`

- [ ] **Step 1: Write failing helper tests**

Create `tests/WebhookSecretVerifierTest.php`:

```php
<?php

declare(strict_types=1);

namespace TH\MAX\Tests;

use PHPUnit\Framework\TestCase;
use TH\MAX\Webhook\WebhookSecretVerifier;

class WebhookSecretVerifierTest extends TestCase
{
    public function testVerifyAcceptsMatchingSecret(): void
    {
        $this->assertTrue(WebhookSecretVerifier::verify('expected-secret', 'expected-secret'));
    }

    public function testVerifyRejectsMismatchedSecret(): void
    {
        $this->assertFalse(WebhookSecretVerifier::verify('wrong-secret', 'expected-secret'));
    }

    public function testVerifyRejectsMissingHeader(): void
    {
        $this->assertFalse(WebhookSecretVerifier::verify(null, 'expected-secret'));
        $this->assertFalse(WebhookSecretVerifier::verify('', 'expected-secret'));
    }

    public function testVerifyRejectsEmptyExpectedSecret(): void
    {
        $this->assertFalse(WebhookSecretVerifier::verify('anything', ''));
    }

    public function testVerifyFromHeadersFindsMaxSecretHeaderCaseInsensitively(): void
    {
        $headers = [
            'x-max-bot-api-secret' => 'expected-secret',
        ];

        $this->assertTrue(WebhookSecretVerifier::verifyFromHeaders($headers, 'expected-secret'));
    }

    public function testVerifyFromHeadersAcceptsArrayHeaderValues(): void
    {
        $headers = [
            'X-Max-Bot-Api-Secret' => ['expected-secret'],
        ];

        $this->assertTrue(WebhookSecretVerifier::verifyFromHeaders($headers, 'expected-secret'));
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
vendor/bin/phpunit tests/WebhookSecretVerifierTest.php
```

Expected: FAIL because `TH\MAX\Webhook\WebhookSecretVerifier` does not exist.

- [ ] **Step 3: Create helper**

Create `src/Webhook/WebhookSecretVerifier.php`:

```php
<?php

declare(strict_types=1);

namespace TH\MAX\Webhook;

class WebhookSecretVerifier
{
    public const HEADER_NAME = 'X-Max-Bot-Api-Secret';

    public static function verify(?string $receivedSecret, string $expectedSecret): bool
    {
        if ($expectedSecret === '' || $receivedSecret === null || $receivedSecret === '') {
            return false;
        }

        return hash_equals($expectedSecret, $receivedSecret);
    }

    public static function verifyFromHeaders(array $headers, string $expectedSecret): bool
    {
        foreach ($headers as $name => $value) {
            if (strcasecmp((string)$name, self::HEADER_NAME) !== 0) {
                continue;
            }

            if (is_array($value)) {
                $value = reset($value);
            }

            return self::verify(is_string($value) ? $value : null, $expectedSecret);
        }

        return false;
    }
}
```

- [ ] **Step 4: Run focused test**

Run:

```bash
vendor/bin/phpunit tests/WebhookSecretVerifierTest.php
```

Expected: PASS.

- [ ] **Step 5: Commit**

Run:

```bash
git add src/Webhook/WebhookSecretVerifier.php tests/WebhookSecretVerifierTest.php
git commit -m "Add webhook secret verifier"
```

---

### Task 5: Update README for v2.0.0 Migration and MAX Guidance

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Update README content**

Add this section after the quick start section and before `## Модули API`:

```markdown
## Миграция на v2.0.0

Версия `v2.0.0` приводит публичный API SDK к актуальной документации MAX API.

Breaking changes:

- Удален `Chats::getAll()`: метод `GET /chats` больше не поддерживается MAX API. Получайте `chat_id` через Webhook-подписки или Long Polling в окружениях разработки.
- Удален `Chats::delete()`: метод `DELETE /chats/{chatId}` отсутствует в актуальной документации MAX API.
- Удален `Bots::update()`: метод `PATCH /me` отсутствует в актуальной документации MAX API.

Новые возможности:

- `UploadTypes` содержит документированные типы загрузки: `image`, `video`, `audio`, `file`.
- `ClipboardButton` добавляет кнопку inline-клавиатуры с типом `clipboard`.
- `WebhookSecretVerifier` помогает проверить заголовок `X-Max-Bot-Api-Secret`.
```

In the Upload examples section, add:

```php
use TH\MAX\Config\UploadTypes;

$uploadUrl = $client->upload()->getUrl(UploadTypes::IMAGE);
```

In the Subscriptions section, add:

```markdown
Для production-окружения MAX рекомендует использовать Webhook. Long Polling через `getUpdates()` оставлен в SDK, потому что метод документирован, но его стоит использовать только для разработки и тестирования.

Webhook endpoint должен использовать HTTPS, доверенный сертификат и порт 443. Если при подписке указан `secret`, проверяйте входящий заголовок `X-Max-Bot-Api-Secret`.
```

Add this example in the Subscriptions section:

```php
use TH\MAX\Webhook\WebhookSecretVerifier;

$isValid = WebhookSecretVerifier::verifyFromHeaders(
    getallheaders(),
    'your_secret_key'
);
```

Add this note near the configuration/base URL section:

```markdown
Документация MAX указывает лимит для `platform-api2.max.ru`: до 30 запросов в секунду.
```

- [ ] **Step 2: Verify removed methods are not advertised**

Run:

```bash
rg -n "getAll\\(|Chats::getAll|Bots::update|Chats::delete|photo|platform-api\\.max\\.ru" README.md
```

Expected: no lines for removed methods, `photo`, or old base domain. Existing generic text that says `getUpdates()` is acceptable.

- [ ] **Step 3: Commit**

Run:

```bash
git add README.md
git commit -m "Document v2 MAX API migration guidance"
```

---

### Task 6: Full Verification

**Files:**
- Verify all modified and created files.

- [ ] **Step 1: Run full PHPUnit suite**

Run:

```bash
vendor/bin/phpunit tests
```

Expected: all tests pass.

- [ ] **Step 2: Run strict removed-method scan**

Run:

```bash
rg -n "function getAll\\(" src/Client/Modules/Chats/Chats.php
rg -n "function delete\\(" src/Client/Modules/Chats/Chats.php
rg -n "function update\\(" src/Client/Modules/Bots/Bots.php
```

Expected: all three commands return no matches.

- [ ] **Step 3: Review final diff**

Run:

```bash
git diff --stat master..HEAD
git diff -- src/Client/Modules/Chats/Chats.php src/Client/Modules/Bots/Bots.php src/Config/UploadTypes.php src/Client/DTO/Messages/Attachments/Buttons/ClipboardButton.php src/Webhook/WebhookSecretVerifier.php README.md tests/StrictDocsSyncTest.php tests/WebhookSecretVerifierTest.php
```

Expected:

- no broad transport rewrite;
- no DTO nullability refactor;
- removed unsupported methods only;
- new classes match the spec;
- README contains migration and production guidance.

---

## Self-Review

- Spec coverage: every requirement in `docs/superpowers/specs/2026-06-25-v2.0.0-strict-docs-sync-design.md` maps to a task.
- Placeholder scan: no unfinished-marker sections.
- Type consistency: test imports match planned classes and namespaces.
- Scope check: plan excludes transport rewrite, DTO nullability refactor, PSR-18 migration, and runtime rate limiting as required.
